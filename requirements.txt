# EpiStream dependencies
# Tested with Python 3.10, CUDA 12.1, single NVIDIA H20 GPU

# ---- Deep learning core ----
torch>=2.1.0
torchvision>=0.16.0

# ---- VLM backbone & LoRA ----
transformers>=4.46.0
peft>=0.12.0
accelerate>=0.26.0

# ---- Vision processing ----
Pillow>=10.0.0

# ---- Numerical / scientific ----
numpy>=1.24.0

# ---- Utilities ----
tqdm>=4.65.0
filelock>=3.12.0

# ---- Training monitoring (optional) ----
tensorboard>=2.14.0

# ---- Qwen2.5-VL tokenizer extras (installed automatically with transformers) ----
# qwen-vl-utils is bundled in recent transformers releases for Qwen2.5-VL;
# if not found, install from: pip install qwen-vl-utils
